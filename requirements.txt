# Core (required)
pydantic>=2.5
numpy>=1.24

# Optional extras (graceful fallbacks exist in code when these are absent)
# Universal I/O Bridge / MCP server
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
# Event bus transport
redis>=5.0
# Vector long-term memory
chromadb>=0.4
# Dynamic LLM routing
litellm>=1.40

# Onboarding TUI
rich>=13.0
# Chat channels
python-telegram-bot>=21.0
discord.py>=2.3

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
